<!DOCTYPE html>
<html data-bs-theme="dark">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Gestionnaire de Tâches{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
        <style>
            :root[data-bs-theme="dark"] {
                --bs-primary: #ffffff;
                --bs-primary-rgb: 255,255,255;
                --bs-body-bg: #000000;
                --bs-body-color: #ffffff;
                --bs-border-color: #262626;
            }
            
            .navbar {
                background-color: #0a0a0a !important;
                border-bottom: 1px solid #FFFFFF!important;
            }
            
            .card {
                background-color: #0a0a0a !important;
                border-color: #262626 !important;
            }
            
            .table {
                --bs-table-bg: #0a0a0a;
                --bs-table-hover-bg: #171717;
            }
            
            .form-control, .form-select {
                background-color: #171717 !important;
                border-color: #262626 !important;
                color: #ffffff !important;
            }
            
            .form-control:focus {
                background-color: #171717 !important;
                border-color: #404040 !important;
                box-shadow: 0 0 0 0.25rem rgba(255,255,255,.15) !important;
            }
            
            .btn-primary {
                background-color: #ffffff !important;
                border-color: #ffffff !important;
                color: #000000 !important;
            }
            
            .btn-primary:hover {
                background-color: #e5e5e5 !important;
                border-color: #e5e5e5 !important;
            }
            
            .btn-outline-primary {
                color: #ffffff !important;
                border-color: #404040 !important;
            }
            
            .btn-outline-primary:hover, 
            .btn-outline-primary.active {
                color: #000000 !important;
                background-color: #ffffff !important;
                border-color: #ffffff !important;
            }
            
            .btn-outline-primary:focus {
                box-shadow: 0 0 0 0.25rem rgba(255,255,255,.15) !important;
            }

            .badge {
                font-weight: 500;
            }
        </style>
    </head>
    <body>
        <nav class="navbar navbar-expand-lg mb-4">
            <div class="container">
                <a class="navbar-brand fw-bold text-primary" href="{{ path('app_task_index') }}">
                    <i class="bi bi-check2-square me-2"></i>Gestionnaire de Tâches
                </a>
            </div>
        </nav>

        <div class="container py-4">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
